# Keyboard shortcuts from System Settings > Keyboard > Keyboard Shortcuts.
# Mackup's built-in `macosx` app would also sync .GlobalPreferences.plist,
# which .chezmoidata/macos.yaml owns.
[application]
name = macOS keyboard shortcuts

[configuration_files]
Library/Preferences/com.apple.symbolichotkeys.plist
