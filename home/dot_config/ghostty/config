# Theme & colors
theme = "Catppuccin Mocha"
bold-is-bright = true
# Disable auto-generated 256-color palette (restores standard xterm colors)
# See https://github.com/ghostty-org/ghostty/discussions/10852
palette-generate = false

# Font
font-family = "Maple Mono NF CN"
font-size = 16
font-feature = calt
font-feature = liga
font-feature = cv01
font-feature = cv02
font-feature = cv07
font-feature = cv33
font-feature = cv34
font-feature = cv62
font-feature = cv64
font-feature = cv98
font-feature = ss01
font-feature = ss03
font-feature = ss05
font-feature = ss07
font-feature = ss08

# Cursor
cursor-style = bar
cursor-style-blink = false
adjust-cursor-thickness = 3
shell-integration-features = no-cursor

# Window
window-padding-x = 8
window-padding-y = 4
window-padding-balance = true
window-padding-color = extend
window-save-state = always
window-step-resize = true
scrollbar = never

# Input & clipboard
copy-on-select = clipboard
clipboard-trim-trailing-spaces = true
clipboard-paste-protection = true
focus-follows-mouse = true
mouse-hide-while-typing = true

# Keybindings
keybind = global:shift+escape=toggle_quick_terminal

# Notifications
bell-features = no-attention,title
desktop-notifications = true

# macOS
macos-option-as-alt = true
macos-hidden = never
# Render colors in Display P3 for vibrant output on macOS
window-colorspace = display-p3
# Thicker font strokes (compensates for thinner Metal rendering)
font-thicken = true

# Split current tab vertically and focus top pane
keybind = super+ctrl+t=new_split:down
keybind = chain=goto_split:up
keybind = chain=resize_split:down,250
